package com.example.drinkapp.domain.use_cases.get_selected_ingredients
/*
import com.example.drinkapp.data.repository.Repository
import com.example.drinkapp.domain.model.Ingredient

class GetSelectedIngredientsUseCase (
    private val repository: Repository
) {
    suspend operator fun invoke(ingredientIds: List<Int>): List<Ingredient>{
        return repository.getSelectedIngredients(ingredientIds = ingredientIds)
    }
}
 */